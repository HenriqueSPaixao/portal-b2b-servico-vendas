# Banco (Cloud SQL compartilhado — senha vem com Sérgio/infra)
DATABASE_URL=postgresql+asyncpg://svc_portal_b2b:CHANGE_ME@136.114.235.212:5432/portal_b2b
DB_SCHEMA=portal_b2b

# Kafka (cluster oficial — 3 brokers)
KAFKA_BOOTSTRAP_SERVERS=10.128.0.2:9092,10.128.0.3:9092,10.128.0.4:9092
KAFKA_CLIENT_ID_PREFIX=vendas

# JWT (mesma secret publicada pelo Guilherme — MS Usuários)
JWT_SECRET=DAJNjnbdaibndiuabdwqbiib24141F15n5j1n
JWT_ISSUER=portal-autenticacao
JWT_AUDIENCE=portal-b2b
JWT_CLOCK_SKEW_SECONDS=60

# Mercado-service específico
MERCADO_MATCHING_TOLERANCE_PERCENT=5
MERCADO_DEFAULT_AUCTION_DURATION_SECONDS=120

# Negociacao-service específico
NEGOCIACAO_AUCTION_POLL_INTERVAL_SECONDS=5

# CORS (front-ends embarcados — inclui gateway oficial + dev local)
CORS_ALLOW_ORIGINS=http://34.8.17.245,http://localhost:3005,http://localhost:3006

# Base URLs dos fronts (build args do Vite — compilados na imagem)
# Em produção, aponte para o gateway oficial; em dev local, basta omitir (fallback localhost)
MERCADO_API_BASE=http://34.8.17.245/api/mercado
NEGOCIACAO_API_BASE=http://34.8.17.245/api/negociacao

# Observabilidade
LOG_LEVEL=INFO
